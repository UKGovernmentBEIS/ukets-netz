package uk.gov.netz.api.account.search.criteria;

/** Common sort fields supported by generic NETZ account search. */
public enum AccountSearchCommonSortField implements AccountSearchSortField {

    OPERATOR_NAME,
    ACCOUNT_ID,
    STATUS
}
